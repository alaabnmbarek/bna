package com.example.secureapp.user;

public enum Role {
    ADMIN,
    CHARGE_DOSSIER,
    RESPONSABLE_CONTENTIEUX,
    PRESTATAIRE_EXTERNE,
    USER // Temporairement pour compatibilité avec l'ancienne base
}
